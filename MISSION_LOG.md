# Mission Log

Format: `[timestamp] [task ID] [DONE/FAILED] [description]`

2026-05-19 A1 DONE .gitignore created (credentials/data/models/mlruns/generated outputs); standalone git repo initialised in fraud-detection/ (separate from parent Machine_Learning_Sample_Project repo); branch main; 2 commits; 29 tracked files, 0 sensitive/large; remote origin=github.com/sgurram15/fraud-detection; pushed. kaggle.json confirmed only at ~/.kaggle/.
2026-05-19 A2 DONE README.md replaced with 8 sections (what/why FCA, architecture ref, local setup Win+Mac, AWS ref, pipeline order, tests, limitations+model card ref, cost warning table).
2026-05-19 A3 DONE docs/AWS_SETUP.md created: 5 steps (root MFA, IAM user, billing alerts, CLI, S3), 4 STOP points summarised, script/cost table. All resources eu-west-2, tag Project=fraud-detection-poc.
2026-05-19 A4-A7,A9 DONE scripts/aws/ created: _common.py, setup_s3.py (private+versioned+SSE-S3), upload_data.py (progress+md5+cost), launch_ec2.py (t3.large AL2023, £0.08/h warn), stop_ec2.py (confirm+savings), run_training_on_ec2.py (ssh pipeline+S3 upload). All compile OK. NOT executed (need boto3/AWS creds/STOP 1-3).
2026-05-19 A8 DONE src/config.py created per spec (LOCAL_DATA_PATH, S3_BUCKET, USE_S3, MODEL_PATH) + data_path()/model_path() resolvers; verified local & S3 modes. SCOPE NOTE: sweeping per-file path rewrite across the verified pipeline deliberately deferred (regression risk; pipeline not re-verified post pandas-3.0 churn) — to be migrated per-file with re-verification, per MISSION rule 4.
2026-05-19 A10 DONE git add -A (gitignore-verified: 0 sensitive/large staged); commit cb43e44; pushed 14c26e2..cb43e44 to origin/main. STOP 4 (remote) was already satisfied. Phase A autonomous tasks complete.
2026-05-20 EXP DONE src/models/explain.py created: SHAP TreeExplainer with explain_prediction (top-5 plain-English reasons with +/- direction), explain_batch (importance/summary/3 dependence plots), generate_fca_explanation (JSON-serialisable audit dict). Smoke-verified at 15k sample: 5 plots + FCA example JSON written; highest-scoring test txn P(fraud)=0.9949, BLOCK, HIGH confidence.
2026-05-20 STOP DONE STOP 1-3 confirmed by user: AWS root MFA on, IAM user 'Shraddha' created (instead of MISSION-spec 'fraud-detection-dev'; permissions to be verified), billing alerts £10/£25 set. STOP 4 already done.
2026-05-20 SESS DONE docs/SESSION_STATE.md created: cold-start primer (project goal, repo, env quirks, module status, key decisions, commands, outstanding work). Memories saved under ~/.claude/projects/.../memory/ for persistent session recovery.
2026-05-20 FIX DONE experiment_tracking._system_info version-lookup except broadened: local pandas 3.0.3 dist-info has no Version field, so importlib.metadata.version("pandas") raised KeyError('Version') and crashed every MLflow run with "MLflow logging failed: 'Version'". Now falls back to "metadata-broken"; verified by round-trip + 100k train_baseline run logging end-to-end. SESSION_STATE refreshed (commit 1653489).
2026-05-20 CFG DONE .claude/settings.local.json untracked (git rm --cached) + added to .gitignore; local IDE/agent config that should never have been committed. Outstanding-work item from SESSION_STATE §8 cleared (commit 3fa1815).
2026-05-20 A8b DONE A8 cross-file path migration begun: train_baseline.py migrated to src/config data_path()/model_path() helpers (was hardcoded local paths). First file of the per-file migration; smoke-verified, others still pending (commit f4c1add).
2026-05-21 A8c DONE A8 cross-file path migration completed across remaining src/ files: handle_imbalance.py, build_features.py, feature_store.py, validate_features.py (data/raw + data/processed via config.data_path()/processed_path()), tune_model.py, validate_model.py, explain.py (models/saved via config.model_path()). Added config.processed_path() helper. USE_S3=true refused at every local-FS entry point (Path.glob/parquet/joblib): explicit guard in load_features() + build_features/feature_store/explain __main__; tune/validate/benchmark inherit train_baseline's import-time refusal. docs/ audit artifacts intentionally stay local. Verified: all 7 modules import clean under USE_S3=false with byte-identical resolved paths; USE_S3=true raises clean NotImplementedError; S3 URIs resolve; feature_store test 3/3 PASS; build_features 20k smoke found data via data_path() and engineered features. No hardcoded data/raw|models/saved literals remain in src/ (test file out of A8 scope). A8 migration thread closed.
2026-05-21 CMP DONE src/models/compare_models.py created: controlled baseline-vs-tuned head-to-head on an IDENTICAL held-out test set (closes the SESSION_STATE §8 "validate baseline not just tuned" gap). Reproduces train_baseline's exact 20% split (100k, seed 42) via prepare_train_test_split(strategy="none"); thresholds chosen on a real-distribution validation slice (not test); both models fed identical imputed test inputs. Split provably correct: reconstructed baseline AUC-ROC 0.9158467510 matches baseline_metrics.json to 1e-6 (threshold-free invariant; runtime-asserted). RESULT — baseline wins: recall@95%-precision baseline 0.3828 vs tuned 0.3535 (margin -2.93pp), AUC 0.9158 vs 0.9036, daily-loss £199,375 vs £208,375. Tuned fails the >=1pp bar -> emitted "TUNED MODEL DOES NOT MEET IMPROVEMENT THRESHOLD..." flag; selected=baseline. Honest caveat surfaced (in stdout + JSON): load_data returns first-n rows so tuned's 40k pool is a subset of baseline's 100k; ~1/3 of the common test set leaked into tuned's training -> comparison is biased IN FAVOUR of tuned, so the baseline verdict is robust. A clean ablation needs equal-sample retrain on the full-grid path. Wrote docs/model_performance/model_comparison.json (both operating points + deltas + caveats); added "## Model Selection" line to docs/model_card.md; logged MLflow run "model-selection-decision" (kept one, deleted a duplicate from a verification re-run). NOTE: the flag string says "Azure ML" per the task spec, but the project's MLOps stack is AWS SageMaker everywhere else — flagged verbatim, worth reconciling.
2026-05-21 BENCH DONE src/models/benchmark_baseline.py created: three-model comparison (trained XGBoost vs TabPFN zero-shot in-context vs majority-class floor) on the exact 20% test slice train_baseline produces, so numbers are directly comparable to deployed baseline. TabPFN constraints (≤500 feats, 10k context) surfaced honestly; AUC threshold-free, recall/precision at F1-optimal per model. Outputs table + docs/model_performance/benchmark_comparison.json (commit cd287bc).
